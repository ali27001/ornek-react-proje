import { createAction } from 'redux-act'

export const filterAccount = createAction('filterAccount')
export const filterAccountSuccessful = createAction('filterAccount Successful')
export const filterAccountFailure = createAction('filterAccount Failure')
